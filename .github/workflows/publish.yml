name: Publish to NuGet

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - run: dotnet restore HtmlToPdf.Renderer.sln

      - run: dotnet build HtmlToPdf.Renderer.sln -c Release --no-restore

      - run: dotnet test tests/HtmlToPdf.Renderer.Tests/HtmlToPdf.Renderer.Tests.csproj -c Release --no-build

      - run: dotnet pack src/HtmlToPdf.Renderer/HtmlToPdf.Renderer.csproj -c Release --no-build -o ./nupkg /p:Version=${{ github.event.release.tag_name }}

      - run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
